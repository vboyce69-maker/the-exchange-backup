rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // UNIVERSAL ACCESS FOR PROTOTYPING
    // This resolves all "Missing or insufficient permissions" console errors
    match /{document=**} {
      allow read, write: if true;
    }
  }
}