rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive read for public marketplace browsing (Listings, Profiles, Auctions)
    // Authenticated write for all collections to ensure accountability
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}