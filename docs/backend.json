
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Core account data for buyers and sellers.",
      "properties": {
        "phoneNumber": { "type": "string" },
        "displayName": { "type": "string" },
        "isVerified": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "role": { "type": "string", "enum": ["buyer", "seller", "admin"] }
      },
      "required": ["phoneNumber", "isVerified", "role"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Public-facing user profile with trust metrics.",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "kycStatus": { "type": "string", "enum": ["unverified", "pending", "verified"] },
        "reliabilityScore": { "type": "number" },
        "transactionsCompleted": { "type": "number" },
        "isFoundingMember": { "type": "boolean" }
      },
      "required": ["id", "kycStatus", "reliabilityScore"]
    },
    "ProductListing": {
      "title": "ProductListing",
      "type": "object",
      "description": "Item listed for sale or auction.",
      "properties": {
        "id": { "type": "string" },
        "sellerId": { "type": "string" },
        "title": { "type": "string" },
        "price": { "type": "number" },
        "status": { "type": "string" },
        "isAuction": { "type": "boolean" },
        "highestBid": { "type": "number" }
      },
      "required": ["id", "sellerId", "title", "price", "status"]
    },
    "EscrowHold": {
      "title": "EscrowHold",
      "type": "object",
      "properties": {
        "transactionId": { "type": "string" },
        "amount": { "type": "number" },
        "status": { "type": "string", "enum": ["held", "released", "disputed", "refunded"] },
        "heldAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "phone", "google"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "description": "User profiles and trust metrics."
        }
      },
      {
        "path": "/publicListings/{listingId}",
        "definition": {
          "entityName": "ProductListing",
          "description": "Marketplace listings."
        }
      },
      {
        "path": "/chatThreads/{threadId}",
        "definition": {
          "description": "Secure trade logs and meetup coordination."
        }
      },
      {
        "path": "/notifications/{id}",
        "definition": {
          "description": "System and market alerts (price drops, bids)."
        }
      },
      {
        "path": "/follows/{id}",
        "definition": {
          "description": "Watchlist for price tracking."
        }
      },
      {
        "path": "/disputes/{disputeId}",
        "definition": {
          "description": "Marketplace safety reports and fund freezes."
        }
      },
      {
        "path": "/moderationQueue/{id}",
        "definition": {
          "description": "Falcon Engine flags for admin review."
        }
      },
      {
        "path": "/referrals/{userId}",
        "definition": {
          "description": "Founding 100 program tracking."
        }
      },
      {
        "path": "/auditLogs/{logId}",
        "definition": {
          "description": "Security audit trail."
        }
      }
    ]
  }
}
