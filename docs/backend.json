
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Core account data for buyers and sellers.",
      "properties": {
        "phoneNumber": { "type": "string" },
        "displayName": { "type": "string" },
        "isVerified": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "lastSignIn": { "type": "string", "format": "date-time" },
        "role": { "type": "string", "enum": ["buyer", "seller", "admin"] },
        "businessInfo": {
          "type": "object",
          "properties": {
            "registrationNumber": { "type": "string" },
            "vatNumber": { "type": "string" },
            "address": { "type": "string" }
          },
          "required": ["registrationNumber", "address"]
        }
      },
      "required": ["phoneNumber", "isVerified", "createdAt", "role"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Public-facing user profile with trust metrics.",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "isIdVerified": { "type": "boolean" },
        "reliabilityScore": { "type": "number" },
        "transactionsCompleted": { "type": "number" },
        "disputeCount": { "type": "number" },
        "registrationDate": { "type": "string", "format": "date-time" },
        "bio": { "type": "string" },
        "profileImageUrl": { "type": "string" }
      },
      "required": ["id", "username", "isIdVerified", "reliabilityScore", "registrationDate"]
    },
    "ProductListing": {
      "title": "ProductListing",
      "type": "object",
      "description": "Item listed for sale or auction.",
      "properties": {
        "id": { "type": "string" },
        "sellerId": { "type": "string" },
        "title": { "type": "string" },
        "price": { "type": "number" },
        "status": { "type": "string" },
        "isAuction": { "type": "boolean" },
        "highestBid": { "type": "number" },
        "isBulk": { "type": "boolean" },
        "quantity": { "type": "number" }
      },
      "required": ["id", "sellerId", "title", "price", "status"]
    },
    "ModerationRecord": {
      "title": "ModerationRecord",
      "type": "object",
      "description": "Log of flagged or blocked content for security review.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "content": { "type": "string" },
        "normalizedContent": { "type": "string" },
        "riskScore": { "type": "number" },
        "decision": { "type": "string" },
        "reason": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pending_review", "resolved", "dismissed"] }
      },
      "required": ["userId", "content", "riskScore", "decision", "timestamp", "status"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous", "phone"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "Private account data and role settings."
        }
      },
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "User profile data."
        }
      },
      {
        "path": "/publicListings/{listingId}",
        "definition": {
          "entityName": "ProductListing",
          "schema": { "$ref": "#/backend/entities/ProductListing" },
          "description": "Marketplace listings."
        }
      },
      {
        "path": "/moderationQueue/{recordId}",
        "definition": {
          "entityName": "ModerationRecord",
          "schema": { "$ref": "#/backend/entities/ModerationRecord" },
          "description": "Queue for flagged and blocked content."
        }
      }
    ]
  }
}
