# The Exchange | Production Deployment Roadmap
**Status:** 90% Ready for Beta

This document outlines the final technical and legal requirements to move from the current functional prototype to a live, South African marketplace.

---

## 1. Technical Prerequisites (The "Final 10%")

### 1.1. Payment Integration (Critical)
- [ ] **Task:** Replace the simulated checkout in `src/app/listings/[id]/page.tsx` with a production API.
- [ ] **Recommended Providers:** Paystack (Capitec Pay support) or Peach Payments.
- [ ] **Escrow Logic:** Implement a webhook listener to confirm fund capture before allowing the seller to initiate the meetup.

### 1.2. Backend Automation
- [ ] **Task:** Deploy Cloud Functions to handle auction conclusion.
- [ ] **Logic:** Every 60 seconds, query `publicListings` where `auctionEndDate <= now` and `status == 'auction_active'`. Update status to `pending_meetup`.

### 1.3. Real-World GPS
- [ ] **Task:** Replace simulated distance in `src/components/LiveMeetupTracker.tsx` with the `navigator.geolocation` API.
- [ ] **Requirement:** Enable SSL (HTTPS) on your production domain to allow browser location access.

---

## 2. Compliance & Legal

### 2.1. POPIA Compliance
- [ ] **Data Retention:** Implement a 30-day auto-deletion policy for unverified KYC selfies to minimize PII surface area.
- [ ] **Officer Appointment:** Appoint a data information officer as per RSA law.

### 2.2. FICA Verification
- [ ] **Task:** Integrated ID numbers should be checked against the Department of Home Affairs (DHA) database using a third-party verification API.

---

## 3. Infrastructure Tuning
- [ ] **Custom Domain:** Connect a `.co.za` or `.com` domain in the Firebase Console.
- [ ] **App Check:** Activate Firebase App Check with reCAPTCHA Enterprise to prevent bot-driven bidding.

---
*Generated by the App Prototyper - Integrity Audit v2.0*
